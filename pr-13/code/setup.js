// Shared React and Recharts destructuring for all components
var { useState, useEffect, useRef, useCallback } = React;
var {
  BarChart,
  Bar,
  XAxis,
  YAxis,
  CartesianGrid,
  Tooltip,
  ResponsiveContainer,
} = Recharts;
